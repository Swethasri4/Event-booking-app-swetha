# Event Booking Application - Architecture Diagram

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                    http://localhost:4200                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP/HTTPS
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ANGULAR FRONTEND (SPA)                        │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Components Layer                                          │  │
│  │  ┌──────────┐ ┌──────────┐ ┌────────────┐ ┌──────────┐  │  │
│  │  │  Login   │ │ Calendar │ │Preferences │ │  Admin   │  │  │
│  │  └──────────┘ └──────────┘ └────────────┘ └──────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Services Layer (API Communication)                        │  │
│  │  ┌──────┐ ┌────────┐ ┌─────────┐ ┌────────┐ ┌─────────┐ │  │
│  │  │ Auth │ │ Booking│ │Timeslot │ │Category│ │UserPref │ │  │
│  │  └──────┘ └────────┘ └─────────┘ └────────┘ └─────────┘ │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Guards & Interceptors                                     │  │
│  │  • AuthGuard (protect routes)                              │  │
│  │  • AdminGuard (admin-only routes)                          │  │
│  │  • AuthInterceptor (add JWT tokens)                        │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ REST API
                              │ (JSON + JWT)
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                  FASTAPI BACKEND (Python)                        │
│                  http://localhost:8000                           │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  API Routes Layer                                          │  │
│  │  /api/auth/* | /api/categories/* | /api/timeslots/*       │  │
│  │  /api/bookings/* | /api/user/preferences                   │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Authentication Middleware                                 │  │
│  │  • JWT Token Validation                                    │  │
│  │  • User Extraction                                         │  │
│  │  • Permission Checking                                     │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Pydantic Schemas (Validation)                            │  │
│  │  • UserCreate, UserLogin, TimeSlotCreate                   │  │
│  │  • BookingCreate, CategoryCreate                           │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Business Logic Layer                                      │  │
│  │  • Authentication (bcrypt, JWT)                            │  │
│  │  • Booking validation                                      │  │
│  │  • Permissions checking                                    │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ SQLAlchemy ORM
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SQLite DATABASE                               │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Tables:                                                   │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │  │
│  │  │    User      │  │  Category    │  │  TimeSlot    │   │  │
│  │  │──────────────│  │──────────────│  │──────────────│   │  │
│  │  │ id (PK)      │  │ id (PK)      │  │ id (PK)      │   │  │
│  │  │ email        │  │ name         │  │ category_id  │   │  │
│  │  │ name         │  │ description  │  │ title        │   │  │
│  │  │ hashed_pwd   │  │ color        │  │ start_time   │   │  │
│  │  │ is_admin     │  └──────────────┘  │ end_time     │   │  │
│  │  └──────────────┘                     └──────────────┘   │  │
│  │                                                            │  │
│  │  ┌──────────────┐  ┌──────────────────────────────────┐  │  │
│  │  │   Booking    │  │   user_preferences (M2M)         │  │  │
│  │  │──────────────│  │──────────────────────────────────│  │  │
│  │  │ id (PK)      │  │ user_id (FK)                     │  │  │
│  │  │ timeslot_id  │  │ category_id (FK)                 │  │  │
│  │  │  (FK, UNIQUE)│  └──────────────────────────────────┘  │  │
│  │  │ user_id (FK) │                                        │  │
│  │  │ booked_at    │  UNIQUE constraint on timeslot_id    │  │
│  │  └──────────────┘  prevents double booking ✅           │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Examples

### 1. User Login Flow

```
[User enters credentials]
         │
         ▼
[Angular Login Component]
         │
         ▼
[Auth Service.login()]
         │
         ▼ HTTP POST /api/auth/login
[FastAPI /auth/login endpoint]
         │
         ▼
[Verify password (bcrypt)]
         │
         ▼
[Generate JWT token]
         │
         ▼ Return {access_token, token_type}
[Angular stores token in localStorage]
         │
         ▼
[Navigate to Calendar]
         │
         ▼
[User is logged in ✅]
```

---

### 2. Booking a Timeslot Flow

```
[User clicks "Book" button]
         │
         ▼
[Calendar Component.bookTimeslot()]
         │
         ▼
[Booking Service.createBooking()]
         │
         ▼ HTTP POST /api/bookings
         │ Header: Authorization: Bearer <JWT>
         │ Body: {timeslot_id: 123}
[FastAPI /bookings endpoint]
         │
         ▼
[Auth Middleware validates JWT]
         │
         ▼
[Extract current user from token]
         │
         ▼
[Check if timeslot exists]
         │
         ▼
[Check if already booked]
         │
         ▼ Database constraint check (UNIQUE)
[Create Booking record]
         │
         ├─ Success → Return booking ✅
         │
         └─ Conflict → 409 error ❌
         │
         ▼
[Angular updates UI]
         │
         └─ Show success/error message
```

---

### 3. Calendar Week Load Flow

```
[User navigates to Calendar]
         │
         ▼
[Calendar Component.ngOnInit()]
         │
         ├─ Calculate current week dates
         ├─ Load user preferences
         └─ Load timeslots
         │
         ▼
[Timeslot Service.getTimeslots()]
         │
         ▼ HTTP GET /api/timeslots?
         │   start_date=2024-01-01&
         │   end_date=2024-01-07&
         │   category_ids=1,2
         │
[FastAPI /timeslots endpoint]
         │
         ▼
[Query database with filters]
         │
         ▼
[Join with Category and Booking tables]
         │
         ▼
[Return TimeSlot[] with booking info]
         │
         ▼
[Angular renders calendar]
         │
         └─ Group by day
         └─ Show availability status
         └─ Display booking info
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│                  Security Layers                         │
├─────────────────────────────────────────────────────────┤
│  1. Frontend                                             │
│     • Route Guards (AuthGuard, AdminGuard)              │
│     • Input validation                                   │
│     • Token in localStorage                              │
│     • Automatic token expiry handling                    │
├─────────────────────────────────────────────────────────┤
│  2. Network                                              │
│     • CORS configuration                                 │
│     • HTTPS in production                                │
│     • JWT in Authorization header                        │
├─────────────────────────────────────────────────────────┤
│  3. Backend                                              │
│     • JWT validation on every request                    │
│     • Permission checks (admin vs user)                  │
│     • Pydantic input validation                          │
│     • Password hashing (bcrypt)                          │
├─────────────────────────────────────────────────────────┤
│  4. Database                                             │
│     • SQLAlchemy ORM (SQL injection prevention)         │
│     • Unique constraints (data integrity)                │
│     • Foreign key constraints                            │
│     • Transaction isolation                              │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 Component Interaction Diagram

```
┌────────────┐
│   Login    │
│ Component  │
└─────┬──────┘
      │ Login success
      ▼
┌────────────┐        ┌─────────────┐        ┌────────────┐
│  Calendar  │ ◄────► │ Preferences │ ◄────► │   Admin    │
│ Component  │  user   │  Component  │  admin  │ Component  │
└─────┬──────┘  can    └──────┬──────┘  only   └─────┬──────┘
      │         switch         │                       │
      │                        │                       │
      │                        │                       │
      ▼                        ▼                       ▼
┌────────────┐        ┌─────────────┐        ┌────────────┐
│  Booking   │        │  UserPref   │        │ Timeslot   │
│  Service   │        │   Service   │        │  Service   │
└────────────┘        └─────────────┘        └────────────┘
      │                        │                       │
      └────────────────────────┴───────────────────────┘
                              │
                              ▼
                      ┌────────────┐
                      │    Auth    │
                      │Interceptor │
                      └─────┬──────┘
                            │ Adds JWT
                            ▼
                      [FastAPI Backend]
```

---

## 🗄️ Database Relationships

```
┌──────────────┐
│     User     │
│──────────────│
│ id (PK)      │◄─────┐
│ email        │      │
│ name         │      │
│ hashed_pwd   │      │ One-to-Many
│ is_admin     │      │
└──────────────┘      │
       │              │
       │ Many-to-Many │
       │ (preferences)│
       │              │
       ▼              │
┌──────────────┐      │
│  Category    │      │
│──────────────│      │
│ id (PK)      │      │
│ name         │      │
│ description  │      │
│ color        │      │
└──────────────┘      │
       │              │
       │ One-to-Many  │
       ▼              │
┌──────────────┐      │
│  TimeSlot    │      │
│──────────────│      │
│ id (PK)      │◄─────┤
│ category_id  │      │
│ title        │      │ One-to-One
│ start_time   │      │ (UNIQUE)
│ end_time     │      │
└──────────────┘      │
       │              │
       │ One-to-One   │
       ▼              │
┌──────────────┐      │
│   Booking    │      │
│──────────────│      │
│ id (PK)      │      │
│ timeslot_id  │──────┘
│  (FK, UNIQUE)│
│ user_id (FK) │
│ booked_at    │
└──────────────┘

Key Relationships:
• User ←→ Category (Many-to-Many via user_preferences)
• Category ←→ TimeSlot (One-to-Many)
• User ←→ Booking (One-to-Many)
• TimeSlot ←→ Booking (One-to-One via UNIQUE constraint)
```

---

## 🎯 Request/Response Flow

### Example: GET /api/timeslots

```
REQUEST:
GET http://localhost:8000/api/timeslots?start_date=2024-01-01&end_date=2024-01-07&category_ids=1,2
Headers:
  Authorization: Bearer eyJhbGc...
  Content-Type: application/json

         │
         ▼
[CORS Middleware]
         │
         ▼
[Auth Middleware]
  • Decode JWT
  • Extract user
  • Validate token
         │
         ▼
[Route Handler]
  • Parse query params
  • Build DB query
  • Execute query
         │
         ▼
[Database Query]
SELECT timeslots.*
FROM timeslots
LEFT JOIN bookings ON timeslots.id = bookings.timeslot_id
LEFT JOIN categories ON timeslots.category_id = categories.id
WHERE timeslots.start_time >= ?
  AND timeslots.start_time <= ?
  AND timeslots.category_id IN (?, ?)
         │
         ▼
[Pydantic Serialization]
  • Convert ORM models to schemas
  • Add computed fields (is_available)
  • Include relations (category, booking)
         │
         ▼
RESPONSE:
[
  {
    "id": 1,
    "title": "Team Meeting",
    "category": {"id": 1, "name": "Cat 1", "color": "#3f51b5"},
    "start_time": "2024-01-01T10:00:00",
    "end_time": "2024-01-01T11:00:00",
    "is_available": false,
    "booking": {
      "id": 10,
      "user_id": 5,
      "user_name": "John Doe",
      "user_email": "john@example.com"
    }
  },
  ...
]
```

---

## 🔄 State Management Flow

```
┌─────────────────────────────────────────────────────┐
│              Angular State Management                │
├─────────────────────────────────────────────────────┤
│                                                      │
│  AuthService                                         │
│  ┌────────────────────────────────────────────┐    │
│  │ currentUserSubject (BehaviorSubject)        │    │
│  │              ▼                               │    │
│  │ currentUser$ (Observable)                   │    │
│  │              ▼                               │    │
│  │ Components subscribe to get updates         │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
│  UserPreferencesService                             │
│  ┌────────────────────────────────────────────┐    │
│  │ preferencesSubject (BehaviorSubject)        │    │
│  │              ▼                               │    │
│  │ preferences$ (Observable)                   │    │
│  │              ▼                               │    │
│  │ Calendar subscribes for filtering           │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
└─────────────────────────────────────────────────────┘

State Updates:
1. Login → Update currentUserSubject
2. All components observing currentUser$ get notified
3. Update preferences → Update preferencesSubject
4. Calendar reloads with new filters
```

---

## 🏃 Deployment Architecture (Production)

```
┌─────────────────────────────────────────────────────┐
│                    INTERNET                          │
└──────────────────┬──────────────────────────────────┘
                   │
                   ▼
          ┌────────────────┐
          │  Load Balancer  │
          │   (nginx/ALB)   │
          └────────┬────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
        ▼                     ▼
┌───────────────┐    ┌───────────────┐
│   Frontend    │    │    Backend    │
│   (Vercel/    │    │  (Heroku/AWS) │
│   Netlify)    │    │               │
│               │    │  ┌─────────┐  │
│  Static Files │    │  │FastAPI  │  │
│  (dist/)      │    │  │Instances│  │
└───────────────┘    │  └────┬────┘  │
                     │       │       │
                     └───────┼───────┘
                             │
                             ▼
                   ┌──────────────────┐
                   │   PostgreSQL     │
                   │   (AWS RDS/      │
                   │   Heroku Postgres)│
                   └──────────────────┘
```

---

## 📈 Scalability Architecture

```
Current (SQLite):
[Angular] ←→ [FastAPI] ←→ [SQLite]
  1 app        1 server     1 file

Small Scale (< 1K users):
[Angular] ←→ [FastAPI] ←→ [PostgreSQL]
  CDN          1 server     1 database

Medium Scale (1K-10K users):
                ┌→ [FastAPI Instance 1] ┐
[Angular CDN] ←┤→ [FastAPI Instance 2] ├→ [PostgreSQL]
                └→ [FastAPI Instance 3] ┘   + [Redis Cache]

Large Scale (10K+ users):
                ┌→ [API Server 1] ┐
[Angular CDN] ←┤→ [API Server 2] ├→ [PostgreSQL Primary]
                └→ [API Server 3] ┘       ↓
                       ↓              [Read Replicas]
                  [Redis Cache]
                  [Message Queue]
```

---

## 🎨 UI Component Tree

```
AppComponent (root)
├── MatToolbar
│   ├── Navigation buttons
│   └── User menu
│
└── RouterOutlet
    │
    ├─ LoginComponent
    │  ├── Login form
    │  └── Register form
    │
    ├─ CalendarComponent
    │  ├── Week navigation
    │  ├── Category filters
    │  └── Week grid
    │      └── Day columns
    │          └── Timeslot cards
    │              ├── Book button
    │              └── Cancel button
    │
    ├─ PreferencesComponent
    │  ├── Category grid
    │  │   └── Category cards
    │  └── Save button
    │
    └─ AdminComponent
       ├── Create form
       └── Timeslots table
```

---

## 🔍 Error Handling Flow

```
[Error Occurs]
      │
      ▼
┌─────────────────┐
│  Where did      │
│  it happen?     │
└────┬────┬───────┘
     │    │
     │    └─────────────────┐
     │                      │
     ▼                      ▼
[Frontend]             [Backend]
     │                      │
     ▼                      ▼
[HTTP Error]          [Python Exception]
     │                      │
     ▼                      ▼
[Interceptor          [FastAPI Exception
 catches]              Handler]
     │                      │
     ▼                      ▼
[Show MatSnackBar]    [Return proper
                       HTTP status]
     │                      │
     └──────────┬───────────┘
                ▼
        [User sees friendly
         error message]
```

---

This architecture supports:
✅ Separation of concerns
✅ Scalability
✅ Security
✅ Maintainability
✅ Testability
✅ Production deployment
